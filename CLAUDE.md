# CLAUDE.md

Project instructions for BRB live in **[AGENTS.md](AGENTS.md)**. Read that file
first; it is the single source of truth and this file is only a pointer, kept so
that tools which auto-load `CLAUDE.md` still find the instructions.

Do not duplicate guidance here — it will drift. Add it to `AGENTS.md`.

Two entry points worth knowing before you start:

- `AGENTS.md` § "Start Here and Resolve Conflicts in This Order" — the document
  priority list.
- `docs/BRB_ART_DIRECTION.md` Part 0 — the gate for any visual work.
