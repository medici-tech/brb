node_modules
.node_modules.provenance-backup/
.next/
out/
dist/
coverage/
output/
playwright-report/
test-results/
*.log
.DS_Store

# Local Claude Code tooling (dev-server launch config, worktree metadata)
.claude/

# Purchased art source packs — full-version LimeZu, redistribution-restricted. Keep local only.
/BRB Assets/
# Curated runtime art — injected at deploy from private storage, never committed (public repo).
/public/assets/brb/
